# Base URL of the DLP API.
# Leave as /api to use the in-browser MSW mock (no backend needed).
# Point it at your deployed API Gateway URL once the backend is live.
VITE_API_BASE_URL=/api

# Set to "false" to disable the MSW mock (e.g. when running against a real API).
VITE_ENABLE_MOCKS=true
